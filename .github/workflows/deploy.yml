name : Build, push and deploy

on:
  push:
    branches:
      - main
      - staging
env:
  AWS_REGION: ap-southeast-2
  ECR_REPOSITORY: hello-php
  
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
          
      - name: Log in to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
        
      - name: Build , tag and push Docker image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.ref_name == 'main' && 'latest' || github.ref_name }}
        run: |
          IMAGE=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker build -t $IMAGE .
          docker push $IMAGE
        
      - name: Verify container starts
        run: |
          docker run -d -p 8081:80 --name test-container  ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.ref_name }}
          sleep 3
          curl --fail http://localhost:8081 || exit 1
          docker stop test-container
          
  deploy-staging:
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.ref_name=='staging'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Copy compose and sql files to EC2 staging
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.STAGING_EC2_HOST }}
          username: ${{ secrets.STAGING_EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "docker-compose.prod.yml,sql/"
          target: "/home/ec2-user/hello-php/"          
      
      - name: Deploy to EC2 staging
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.STAGING_EC2_HOST }}
          username: ${{ secrets.STAGING_EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd /home/ec2-user/hello-php
            #Update image tag to staging
            sed -i 's|hello-php:latest|hello-php:staging|g' docker-compose.prod.yml
            docker stop $(docker ps -q) 2>/dev/null || true
            docker rm $(docker ps -aq) 2>/dev/null || true
            aws ecr get-login-password --region ap-southeast-2 | docker login --username AWS --password-stdin ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.ap-southeast-2.amazonaws.com  
            docker compose -f docker-compose.prod.yml pull
            docker compose -f docker-compose.prod.yml up -d
            
  deploy-production:
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.ref_name=='main'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Copy compose and sql files to EC2 production
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "docker-compose.prod.yml,sql/"
          target: "/home/ec2-user/hello-php/"          
      
      - name: Deploy to EC2 production
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd /home/ec2-user/hello-php
            docker stop $(docker ps -q) 2>/dev/null || true
            docker rm $(docker ps -aq) 2>/dev/null || true
            aws ecr get-login-password --region ap-southeast-2 | docker login --username AWS --password-stdin ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.ap-southeast-2.amazonaws.com  
            docker compose -f docker-compose.prod.yml pull
            docker compose -f docker-compose.prod.yml up -d            
